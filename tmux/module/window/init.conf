# Window-related bindings and options.

unbind c

set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g allow-rename on
run-shell "python3 ~/.config/tmux/module/window/bindings.py"
